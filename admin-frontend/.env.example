# Backend API base URL (read at build time by Create React App).
# Leave unset for local development — it defaults to http://localhost:8000.
# For a production build, set this to your deployed backend URL, e.g.:
#   REACT_APP_API_URL=https://vawc-backend.onrender.com
REACT_APP_API_URL=
